[project]
name = "drone-landing-safety"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flax>=0.12.7",
    "jax>=0.10.2",
    "jaxlib>=0.10.2",
    "optax>=0.2.8",
]
